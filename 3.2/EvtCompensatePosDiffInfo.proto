syntax = "proto3";

import "Vector.proto";

message EvtCompensatePosDiffInfo {
  Vector cur_pos = 14;
  uint32 entity_id = 11;
  int32 face_angle_compact = 10;
  uint32 cur_hash = 4;
  uint32 normalized_time_compact = 3;
}
